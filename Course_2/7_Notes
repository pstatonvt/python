7.2 notes:

for line in fhand:
	line = line.rstrip()
	if line.startswith('From: '):
		print(line)

''''

fname = input('Enter a file name: ')
try:
	fand = open(fname)
except:
	print('File cannot be opened:',fname)
	quit()

''''

